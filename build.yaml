builders:
  app_info:
    import: "package:ux/builder.dart"
    builder_factories:
      - "appInfoBuilder"
    build_extensions:
      "$package$":
        - "lib/app_info.g.dart"
    auto_apply: dependents
    build_to: source
